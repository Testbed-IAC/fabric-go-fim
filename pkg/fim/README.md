# pkg/fim

Package `fim` contains module-level FIM compatibility metadata.

## API

- `Version`

Use this package when a caller needs to display or check the FIM compatibility
version exposed by this module.

Topology construction belongs in `pkg/topology`; typed records belong in
`pkg/sliver`; catalog data belongs in `pkg/catalog`.
